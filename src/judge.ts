//Importing the array type to be used as arrgument type control.
import {Hand} from './types'

//using this function we can make decisions on hands and comper them. 😀 
export const judgeHands=(handOne: Hand, handTwo: Hand)=>{
    console.log(handOne);
    
    
}